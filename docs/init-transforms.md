```
pip install transformers  ## instalacion de transforms
pip install transformers[torch] ## For CPU-support only, you can conveniently install 🤗 Transformers and a deep learning library in one line.
pip install transformers[tf-cpu] ## Transformers and TensorFlow 2.0:
pip install transformers[flax] ## Transformers and Flax

# or

pip install git+https://github.com/huggingface/transformers ## Install with source




pip install transformers==4.14.1
pip install bitsandbytes-cuda115==0.26.0.post2
pip install datasets==1.16.1
```